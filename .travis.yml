language: python
python:
  - "2.7"
# command to install dependencies
install: "pip install -r requirements-dev.txt"
# command to run tests
script: 
  - flake8 awssecuritypicturesk --ignore=E501
  - python ./setup.py install

